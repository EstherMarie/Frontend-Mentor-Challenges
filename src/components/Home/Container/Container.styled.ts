import styled from 'styled-components';

export const Container = styled.div`
  max-width: 1100px;
  width: 100%;

  padding-inline: 22px;
  margin: 0 auto;

  font-family: 'Barlow', sans-serif;
  color: #1c2022;

  h1,
  h2,
  h3 {
    font-weight: 400;
  }
`;
